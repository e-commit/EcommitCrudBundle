{{ crud_search_form_start(crud, {}, {'class': 'form-inline'}) }}
    {{ form_row(crud.searchForm.username) }}
    {{ form_row(crud.searchForm.firstName) }}
    {{ form_row(crud.searchForm.lastName) }}

    <div>
        {{ crud_search_form_submit(crud) }}
        {{ crud_search_form_reset(crud) }}
    </div>
</form>
